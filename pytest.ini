[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    unit: Unit tests (pure logic, no external deps)
    integration: Integration tests (need DB / HTTP)
    slow: Slow tests (e.g. network calls)

addopts = -v --tb=short
